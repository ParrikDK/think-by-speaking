# secrets & data
.env
*.db
*.db-journal
*.db-wal
*.db-shm
logs/
*.log
*.pid

# python
.venv/
venv/
__pycache__/
.pytest_cache/
.ruff_cache/

# node
node_modules/

# build output (regenerated by `npm run build`)
backend/app/static/
!backend/app/static/.gitkeep

# os
.DS_Store

# local macOS launcher (Automator app bundle) — not part of the product
Tutor Server.app/

# buffer/ — audit workspace with conversation logs, screenshots, raw captures.
# NEVER commit this folder: it can contain transcripts with real API keys.
buffer/
